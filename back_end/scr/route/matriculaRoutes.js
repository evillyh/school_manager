const express = require('express');
const router = express.Router();

// Rota de exemplo para matrícula - manter como placeholder
router.get('/', (req, res) => res.json({ mensagem: 'Rota de matrícula (placeholder)' }));

module.exports = router;
